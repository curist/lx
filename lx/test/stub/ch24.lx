fn fib(n) {
  if n < 2 {
    return n
  }
  fib(n - 1) + fib(n - 2)
}

let start = Date.time()
let x = 20
println("fib(", x, "):", fib(x))
println("time:", Date.time() - start)
println()

println(int(123.456))
