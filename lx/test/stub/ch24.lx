fn fib(n) {
  if n < 2 {
    return n
  }
  fib(n - 1) + fib(n - 2)
}

let start = Date.now()
let x = 20
println("fib(", x, "):", fib(x))
println("time:", Date.now() - start)
println()

println(int(123.456))
