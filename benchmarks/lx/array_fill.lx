fn arrayFill(n) {
  let arr = range(n) 
  for let i = 0; i < n; i = i + 1 {
    arr[i] = i
  }
  let sum = 0
  for let i = 0; i < n; i = i + 1 {
    arr[i] = arr[i] + 1
    sum = sum + arr[i]
  }
  sum
}

let raw = Lx.args[3]
let n = raw and tonumber(raw) or 0
println(arrayFill(n))
