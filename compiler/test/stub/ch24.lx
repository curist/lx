fn fib(n) {
  if n < 2 {
    return n
  }
  fib(n - 1) + fib(n - 2)
}

let start = Date.time()
let x = 20
print("fib(", x, "):", fib(x))
print("time:", Date.time() - start)
print()

print(int(123.456))
