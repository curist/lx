let a = collect x in range(11) { x * x }

each(a, _1(print))

(collect x in range(101) { x * x })
  ->fold(0, fn(a, b) { a + b })
  ->print()

fn add(a, b) { a + b }
print("sum of 0~100:", range(101)->fold(0, add))

print("...and... hello world!!")
